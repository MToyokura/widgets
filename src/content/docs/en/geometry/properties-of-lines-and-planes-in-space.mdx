---
title: Properties of Lines and Planes in Space
sidebar:
  order: 1030
---

import PerpendicularLineAndPlane from "@/components/three/PerpendicularLineAndPlane.svelte";
import TwoLinesParallelToSameLine from "@/components/three/TwoLinesParallelToSameLine.svelte";
import TwoLinesPerpendicularToSameLine from "@/components/three/TwoLinesPerpendicularToSameLine.svelte";
import RenderingSettings from "@/components/three/shared/RenderingSettings.svelte";

<RenderingSettings locale="en" client:visible />

## A line perpendicular to a plane

To say that a line is perpendicular to a plane, it is sufficient to show that it is perpendicular to two intersecting lines on that plane. Once this condition is met, the line is defined as perpendicular to the entire plane, and as a result, the angle it makes with every line existing on the plane is 90 degrees.

<PerpendicularLineAndPlane
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

## Two lines parallel to the same line

Two lines parallel to the same line are parallel to each other.

<TwoLinesParallelToSameLine
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

## Two lines perpendicular to the same line

In space, two lines perpendicular to the same line are not necessarily parallel to each other.

<TwoLinesPerpendicularToSameLine
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>
