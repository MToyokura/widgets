---
title: Lines and Planes in Space
sidebar:
  order: 1010
---

import PerpendicularLineAndPlane from "@/components/three.js/PerpendicularLineAndPlane.svelte";
import ThreePointsNotCollinear from "@/components/three.js/ThreePointsNotCollinear.svelte";
import LineAndPointNotOnLine from "@/components/three.js/LineAndPointNotOnLine.svelte";
import IntersectingLinesOnPlane from "@/components/three.js/IntersectingLinesOnPlane.svelte";
import IntersectingLinesAbovePlane from "@/components/three.js/IntersectingLinesAbovePlane.svelte";
import ParallelLines from "@/components/three.js/ParallelLines.svelte";
import SkewPosition from "@/components/three.js/SkewPosition.svelte";
import RenderingSettings from "@/components/three.js/shared/RenderingSettings.svelte";

<RenderingSettings locale="en" client:visible />

## Determination of a Plane

A plane is determined when it satisfies any of the following conditions:

- Three points not on the same line
- A line and a point not on that line
- Two intersecting lines

### Three points not on the same line

<ThreePointsNotCollinear
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

### A line and a point not on that line

<LineAndPointNotOnLine
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

### Two intersecting lines

<IntersectingLinesOnPlane
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

## Positional Relationship Between Two Lines

### Intersecting

<IntersectingLinesAbovePlane
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

### Parallel (Not Intersecting)

<ParallelLines
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

### Skew Position (Neither Intersecting nor Parallel)

<SkewPosition
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

## A line perpendicular to a plane

To assert that a line is perpendicular to a plane, it is sufficient to show that it is perpendicular to two intersecting lines on that plane. Once this condition is met, the line is defined as being perpendicular to the entire plane, and as a result, the angle it forms with every line existing on the plane will be 90 degrees.

<PerpendicularLineAndPlane
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>
