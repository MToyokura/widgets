---
title: Propiedades de rectas y planos en el espacio
sidebar:
  order: 1030
---

import PerpendicularLineAndPlane from "@/components/three/PerpendicularLineAndPlane.svelte";
import TwoLinesParallelToSameLine from "@/components/three/TwoLinesParallelToSameLine.svelte";
import TwoLinesPerpendicularToSameLine from "@/components/three/TwoLinesPerpendicularToSameLine.svelte";
import RenderingSettings from "@/components/three/shared/RenderingSettings.svelte";

<RenderingSettings locale="es" client:visible />

## Recta perpendicular a un plano

Para determinar que una recta es perpendicular a un plano, basta con demostrar que es perpendicular a dos rectas que se intersecan en dicho plano. Una vez cumplida esta condición, la recta es perpendicular a todos los elementos del plano; por consiguiente, el ángulo que forme con cualquier recta contenida en el plano será de 90 grados.

<PerpendicularLineAndPlane
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

## Dos rectas paralelas a una misma recta

Dos rectas que son paralelas a una tercera recta son paralelas entre sí.

<TwoLinesParallelToSameLine
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>

## Dos rectas perpendiculares a una misma recta

En el espacio, dos rectas perpendiculares a una misma recta no son necesariamente paralelas entre sí.

<TwoLinesPerpendicularToSameLine
  client:visible
  pixelRatioCapControlId="geometry-pixel-ratio-cap-toggle"
  antialiasControlId="geometry-antialias-toggle"
/>
